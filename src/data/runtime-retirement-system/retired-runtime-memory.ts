export type RetiredRuntimeMemory = {
  retiredMemoryLine: string;
};

export function resolveRetiredRuntimeMemory(): RetiredRuntimeMemory {
  return {
    retiredMemoryLine: "退休的 runtime 不留“高级缺席叙事”，只留空位。",
  };
}
