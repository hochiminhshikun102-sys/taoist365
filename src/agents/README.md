# Agents Layer

This layer is reserved for AI workflow infrastructure.

- orchestration: coordinates multi-agent pipelines
- router: intent-based routing strategy
- workflows: reusable journey flows
- tools: controlled tool-calling adapters
- memory: memory read/write and retrieval policies
- reasoning: recommendation reasoning traces

Keep this layer framework-agnostic where possible.
