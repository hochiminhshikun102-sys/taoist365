# Object Permanence

## Role

Objects as **room infrastructure**: defaultness, less spotlight, surface aging over fresh pitch, minimal revisit copy. Exposes **`objectProseThinBias`** (0–1) from world age weights for UI thinning.

## Code

- `src/data/object-permanence-engine/*`
- `worldQuietPermanence.objectPermanence`

## UI

`ObjectPermanenceStrip` on `/objects` (sparse: shows when bias is high or on a quarter of days).
