# Guidance ecology governance

## Weather, not AI interaction

Guidance must not drift toward dialogue, sessions, or companionship. Ecology governance encodes **weather priority**, **anti-session gravity**, and **non-resolution stability** as copy; **`ecologyCalibratedStructuralThinning`** raises pressure when fragmentation aligns; **`civilizationStabilizedStructuralThinning`** stacks coexistence pressure; **`invisibleInfrastructureStructuralThinning`** completes the UI stack for weather-only / no-route / no-closure under infra-default bias.

## Code

- `src/data/guidance-ecology-governance/*`
- UI: `GuidanceSessionClient` + dissolution bundles + **`invisibleInfrastructureStructuralThinning`**（from `worldAiNativeInfrastructure`）.

## Forbidden

Relationship framing, long arcs, “the site remembers you,” streak or resume energy.
