# Next Steps

## QUICK CONTEXT BOOTSTRAP

- Documentation-first: deepen **quiet ritual atmosphere** and runtime coherence before new product-like surfaces.
- Next work: structural absence tuning, bundle precedence, daily block budgets, object visibility—still static and deterministic.
- Reject “quick wins” that add chat energy, dashboards, or dense UI; do not code ahead of brain updates.
- Protect **low-signal humanity**, **ritual calm**, and **breathable interfaces** over novelty.

## THIS DOCUMENT DEFINES

The maintenance-safe backlog and guardrails for maturing the **Taoist-inspired lifestyle guidance system** without losing **emotional continuity**.

## CORE PRINCIPLES

- Before large new surfaces: keep runtime coherent, reduce over-expression, preserve mature **low-signal humanity**.
- No feed-like surfaces, account/memory backends, assistive-product drift, aesthetic escalation, or noisy density.

## SYSTEM DETAILS

### Recommended sequence

1. **`invisibleInfrastructureStructuralThinning` audit** — UI **唯一**终端变薄；**勿**调 **`civilizationStabilizedStructuralThinning`** 冒充 UI-final（见 `terminology-governance.md`）。若 Daily / Guidance 过空，先调 `extreme-invisible-infrastructure-thinning-pass.ts`，再 `extreme-civilization-stabilization-thinning-pass.ts`，再 ecology / governance / ambient / base passes（`invisible-infrastructure.md`, `ambient-civilization-equilibrium.md`, `world-ecology-calibration.md`, `runtime-ecology-principles.md`, `long-term-non-deformation.md`）。
2. **`permanencePass` audit** — if Daily feels too empty on common days, lower `proseCollapseBias` weights in `permanence-thinning-pass.ts` or gate flags (`post-product-internet.md`, `default-presence-philosophy.md`).
3. **`worldQuietPermanence` tuning** — if Guidance feels over-collapsed, raise thresholds in `guidance-dissolution/system.ts` and/or `guidance-quiet-collapse/system.ts` (`guidance-dissolution.md`, `guidance-collapse-policy.md`).
4. **`worldMaturity` copy audit** — keep freeze / mail / object / aging lines flat; confirm no overlap with `MailTemporalPresence` that reads as duplicate paragraphs (`true-mail-continuity.md`, `world-freeze.md`).
5. **Retirement + anti-meta copy audit** — scan `runtime-retirement-system` and `anti-system-self-awareness` for accidental cleverness; keep lines flat (`anti-meta-governance.md`, `runtime-retirement-engine.md`).
6. **Browser reality + stability copy audit** — read `browser-reality-engine` lines for accidental nostalgia / brand tone; keep hostname lines mechanical (`hostname-familiarity.md`, `real-internet-aging.md`, `quiet-internet-permanence.md`, `browser-reality-principles.md`).
7. **World regulation tuning** — thresholds in `runtime-fatigue-balancer` / `cross-runtime-suppression` / `narrative-overflow-runtime`; confirm Guidance never loses weather + context on max collapse; confirm Daily never devolves to disclaimers-only on thin days.
8. **Structural absence density** — calibrate with `pageEnergy` + `silenceThresholds`; keep pages usable when blocks are absent; stronger maturity without readability loss.
9. **Runtime conflict matrix** — precedence when bundles overlap (see `runtime-hierarchy.md`; e.g. `lowSignalHumanity.guidanceFalloff` vs `structuralSilence.guidanceFragmentation`; inertia `guidanceMinimal` vs `worldRegulation`; `guidanceDissolution` + `guidanceQuiet` vs `runtimeRetirement.guidanceRetirement`).
10. **Residual channel UI** — map `residualBalance.preferredSlots` to explicit per-page residual components (today: copy-level gates only on Daily/Mail in part).
11. **Ritual pages** — optional `ritualUnderstatement` + `sparseRitualTraces` hooks on `RitualsTemporalEcho` / ritual shells without feed energy.
12. **`proseDensityGovernor` hooks** — optional thinning of dense stacks when `worldMaturity.stillnessGovernor.proseDensityGovernor` is high (`stillness-governance.md`).
13. **`reinforcementScalar` / `objectProseThinBias` hooks** — optional UI thinning using `worldQuietPermanence` (`world-stillness-reinforcement.md`, `object-permanence.md`).
14. **Docs checklist (optional)** — PR prompts: runtime fields changed? brain updated? `decisions-log.md` updated?

## MAINTENANCE NOTES

- Finishing a planned item → append `decisions-log.md` and revise priorities here.
- Start conflict-matrix and budget work in this file, then reflect outcomes in `runtime-systems.md` / `architecture.md` as they stabilize.
