# Guidance Collapse Policy

## Layers

1. **`worldQuietPermanence.guidanceDissolution`** — first tightening pass.
2. **`worldPostProductContinuity.guidanceQuiet`** — second pass: `noticingHardCap`, `forceRoutesRetirement`, `hideSoftClosure`, `weatherOnlyCollapse` flags + charter lines.

## Target end state

Guidance may approach **weather + at most one noticing + plain exit**—still a valid page, not a chat-shell session.

## Forbidden

Therapy, coach, oracle, companion AI, parasocial closure theater.

## UI

`GuidanceSessionClient` applies both layers to `maxLinesRaw`, `skipRoutes`, and `hidePauseBlock`.
