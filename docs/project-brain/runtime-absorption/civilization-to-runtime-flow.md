# Civilization to runtime flow（文明 → 运行时）

## Pipeline

Brain → data bundles (`src/data/*`) → `useWorldRuntime()` → CSS / density / absence gates → UI reads **numbers & short residue**, not essays.

## Related

- `runtime-softness-consumption.md`、`../runtime-systems.md`
