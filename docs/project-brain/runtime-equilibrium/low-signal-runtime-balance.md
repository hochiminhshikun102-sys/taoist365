# Low-signal runtime balance（低信号运行时平衡）

## Goal

Channels split **low bandwidth** budget — guidance weather borrows from mail/objects prose budget conceptually。

## Implementation

Ecology + civilization intermediate passes + infra terminal pass — chain in `runtime-hierarchy.md`。
