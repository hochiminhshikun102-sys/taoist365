# Long-Term Browser Civilization Continuity Kernel

This file is the kernel handoff for Reverent Inquiry. It does not authorize new pages, new runtime layers, organism proliferation, feature kernels, or atmosphere density.

Executable check:
- `npm run guardrail:kernel`

## Continuity Kernel Governance

The continuity kernel is the smallest recoverable core for long-term operation. It keeps brand, freeze, archive, production assets, runtime topology, operating memory, and Browser Air continuity aligned after thread changes, workflow changes, long pauses, or developer handoff.

The kernel does not coordinate features. It coordinates continuity.

## Kernel Runtime Topology

Runtime topology is frozen through:
- `src/lib/use-world-runtime.ts`
- `src/config/runtime-infrastructure-guardrail.json`
- `npm run guardrail:runtime`

The topology boundary is:
- 122 `src/runtime` TypeScript files
- 113 top-level `src/data` directories
- 26 `WorldRuntime` fields
- 26 `buildRuntime()` resolver steps

Future runtime work should consolidate before adding. New top-level fields or resolver steps are kernel drift unless paired with explicit retirement or consolidation.

## Kernel Memory

Kernel memory is engineering memory, not chat memory.

Recovery order:
1. `npm run guardrail:kernel`
2. `docs/project-brain/civilization-freeze-notes.md`
3. `docs/project-brain/browser-civilization-governance.md`
4. `docs/project-brain/browser-civilization-infrastructure.md`
5. `docs/project-brain/browser-civilization-runtime-infrastructure.md`
6. `docs/project-brain/browser-civilization-operating-layer.md`
7. `docs/project-brain/browser-civilization-continuity-kernel.md`

If archive-class docs conflict with the executable guardrails, the guardrails win.

## Windkeep Kernel Infrastructure

Windkeep remains the object continuity kernel: shelf continuity, keeper traces, ordinary object persistence, Quiet Receiving, and object drift continuity.

It must not become an object system, marketplace layer, inventory backend, ownership engine, auction space, or collectible psychology.

## Driftbox Kernel Infrastructure

Driftbox remains the low-frequency continuity kernel: sparse, unresolved, quietly drifting, browser-native, and non-assistant.

It must not carry assistant residue, AI expectations, interaction gravity, conversational pull, emotional stickiness, or response pressure.

## The Courier Kernel Governance

The Courier remains quiet organism continuity: browser micro-presence, low attention, lightly irregular, and not featureized.

Organisms must not become mini products, utility systems, dashboards, coordinated modules, or app-like actors.

## Invisible Kernel Coordination

The kernel should be invisible. Users should not feel architecture, orchestration, operating intelligence, or system ego.

Keep:
- quiet continuation
- low-consciousness coordination
- browser-native persistence
- ordinary exits
- subtle recurrence

Avoid:
- architecture performance
- visible orchestration
- intelligence display
- infrastructure exhibition
- self-describing systems

## Reality-Compatible Kernel

The kernel must coexist beside ordinary life. It cannot become emotionally immersive, dependency-oriented, AI-attached, sanctuary forming, reality replacing, or psychologically adhesive.

The kernel is stable when the site can be left alone without losing meaning.

## Kernel Stability Automation

Before production changes, run:
- `npm run guardrail:kernel`
- `npm run lint`
- `npm run build`

This catches runtime inflation, orchestration drift, infrastructure pollution, archive leakage, assistant creep, marketplace creep, dashboard creep, and emotional dependency drift through existing guardrail layers.
