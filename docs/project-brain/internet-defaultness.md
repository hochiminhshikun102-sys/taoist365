# Internet Defaultness

## Definition

**Real internet defaultness** is the posture that Taoist365 is an **ordinary URL**—a **default browser place**, not a design experiment, AI showcase, or art project.

## Implementation

- Path: `src/data/real-internet-defaultness/`
- `resolveRealInternetDefaultnessBundle(structuralSilence)` — `ordinaryPresenceLine`, `defaultBrowserPlaceLine`, `nonPerformativeInternetLine`, etc.

### Extension（默认存在阶段）

- `src/data/internet-defaultness-engine/*` → `useWorldRuntime().worldDefaultExistence.internetDefaultnessEngine`（`urlDefaultnessBias` 与额外「默认网址」文案；**不**替代 `real-internet-defaultness`，只叠一层 charter）。

## UI

- `BrowserHostnameFoot` includes `ordinaryPresenceLine` beside hostname / sediment copy.

## Why it matters

Finishes the arc: **not product**, **not brand performance**—**infrastructure-like presence**.
