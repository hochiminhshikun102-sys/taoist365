# Silence priority

## Silence as highest priority

Default stance: **withhold** optional paragraphs, echoes, routes, and strips before adding reassurance. Absence is scheduled (`structuralAbsence`, breathing modes)—never loading-shaped.

## Code

- `src/data/silence-priority-system/*` — charter lines for retreat, residue thinning, silence budget.
- Thinning: ecology calibration raises pressure via **`ecologyCalibratedStructuralThinning`**; civilization stabilization stacks **`civilizationStabilizedStructuralThinning`**; **`invisibleInfrastructureStructuralThinning`** applies UI-final closure when fragmentation + breathing + coexistence + infra-default pressure demand it.

## Relationship to `silence-governance.md`

`silence-governance.md` maps structural/density/regulation layers; **`silence-priority-system`** names the **priority rule** (“prefer not to express”) that the ecology pass implements numerically.
