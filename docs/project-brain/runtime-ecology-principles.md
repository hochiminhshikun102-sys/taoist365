# Runtime ecology principles

## Mutual restraint

1. **No simultaneous peaks** — strong guidance weather modes borrow bandwidth from mail echoes and object prose (via shared thinning bias), not via new widgets.
2. **Fatigue is global** — tired worlds thin strips and prose together (`runtime-ecology.md`, governance pass).
3. **Silence wins ties** — when pressure is ambiguous, the stack ORs toward ultra-thin (`silence-priority.md`).
4. **No meta narration** — calibration changes thresholds, not footer copy about “the world evolving.”

## Stack order

`structuralThinning` → `ambientStructuralThinning` → `governedStructuralThinning` → **`ecologyCalibratedStructuralThinning`** → **`civilizationStabilizedStructuralThinning`** (intermediate / non-final) → **`invisibleInfrastructureStructuralThinning`** (UI-final only — `terminology-governance.md`).
