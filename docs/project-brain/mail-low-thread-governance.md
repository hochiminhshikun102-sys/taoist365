# Mail low-thread governance

## Not a communication feature

Mail reads as **shelf correspondence**: low thread, background pressure, resolution retired quietly—no service pipeline tone.

## Code

- `src/data/mail-low-thread-governance/*`
- UI: `MailTemporalPresence`, `MailContinuityStrip` gate on **`invisibleInfrastructureStructuralThinning`** (`mailRetireDenseProcessBlock`, `combinedProseBias`).

## Goal

Continuity without inbox theater or reunion framing.
