# Ambient civilization equilibrium

## QUICK CONTEXT BOOTSTRAP

- Equilibrium here means **coexistence without escalation**: multiple pressures (breathing, fragmentation, prose collapse, cross-runtime suppression) resolve into one **final thinning field**.
- Visitors should not perceive “layers”; they perceive **a calmer room** on heavy days.

## THIS DOCUMENT DEFINES

The conceptual bridge between **`worldEcologyCalibration`** (mutual limits) and **`worldCivilizationStabilization`** (long-run coexistence and extra thinning).

## CORE PRINCIPLES

- **Single UI surface for thinning**: components read **`invisibleInfrastructureStructuralThinning`** end-to-end（`civilizationStabilizedStructuralThinning` = **intermediate / non-final** stack directly below — `terminology-governance.md`）。
- **No competing narrators**: ecology output remains intermediate; civilization output is **UI-effective**.
- **Deterministic fairness**: same day key → same pass values; no personalization shortcuts.

## CODE ANCHOR

- Chain: `structuralThinning` → `ambientStructuralThinning` → `governedStructuralThinning` → `ecologyCalibratedStructuralThinning` → `civilizationStabilizedStructuralThinning` → **`invisibleInfrastructureStructuralThinning`**.
- See `runtime-ecology-principles.md`, `runtime-hierarchy.md`, `extreme-civilization-stabilization-thinning-pass.ts`.

## WHY EQUILIBRIUM BEATS ACCUMULATION

Adding bundles without a final compositor produces **entropic UI**—everything talks. Equilibrium passes convert many signals into **one disciplined thinning vector**.

## MAINTENANCE NOTES

- Document new pressures in ecology/civilization bundles and wire them into passes—avoid one-off component hacks.
