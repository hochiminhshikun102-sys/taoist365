# Guidance 溶解哲学（与后交互层）

## 三层（从先到后）

1. `worldDensity` / `structuralSilence` — 结构缺席与折叠。  
2. `worldQuietPermanence.guidanceDissolution` + `worldPostProductContinuity.guidanceQuiet`。  
3. `worldAiNativeInfrastructure.invisibleInfrastructureStructuralThinning`（ambient → governance → ecology → civilization 叠化）— **weather-only**、**硬关 routes/closure** 等重度日。

## 目标体感

不是「AI 在回应」，而是 **网址里有一点点人的温度**；禁止 therapy / coach / oracle / 陪伴式语气。
