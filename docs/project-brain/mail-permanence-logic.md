# Mail 永久低线程逻辑

## 意图

Mail 进入 **长期低线程**：默认、短、像沉积；不像功能、不像工单、不像关系运营。

## 代码

- `src/data/mail-permanent-thread/*` → `worldDefaultExistence.mailPermanentThread`（文案层；与 `true-mail-continuity` 等并存）。  
- `MailTemporalPresence` 在 `invisibleInfrastructureStructuralThinning.mailRetireDenseProcessBlock` 时进入更薄模式（与 `mailUnderstatement` 叠加）。

## 禁止

假亲密、CRM、「我们记得你」、留存话术。
