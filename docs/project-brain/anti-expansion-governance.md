# 反扩张治理（Anti-Expansion Governance）

## 意图

世界 **自动抵抗** 功能压力、runtime 膨胀、prose 膨胀、meta 自炫与「AI 感」——通过确定性偏置与 UI 变薄，而不是加新控制台。

## 代码

- `src/data/world-quiet-equilibrium/*` → `worldDefaultExistence.quietEquilibrium`（`equilibriumPressure` 喂给 `structuralThinning`）。  
- 与 `world-stillness-governor`、`runtime-retirement-engine` 等互补，不重复注册顶层 hook。

## 原则

**越来越默认**，不是越来越强。
