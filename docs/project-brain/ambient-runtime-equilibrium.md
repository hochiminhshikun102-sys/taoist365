# Ambient runtime equilibrium

## Equilibrium without spectacle

Ambient equilibrium means runtimes settle into **background coexistence**: nobody wins loud at the expense of total chatter. The calibration pass lifts `combinedProseBias` when fragmentation + breathing + foreground pressure say the room is crowded.

## Code anchor

- Data line bundle: `world-ecology-calibration/ambient-runtime-equilibrium.ts`.
- Numeric equilibrium: `resolveEcologyCalibratedStructuralThinningPass`.

## Forbidden

Celebrating equilibrium on the page (“everything is calmer now”)—adjust thresholds instead.
