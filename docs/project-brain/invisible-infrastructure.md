# Invisible infrastructure（不可见基础设施）

## 快速对齐

- **Invisible infrastructure pass** = **`invisibleInfrastructureStructuralThinning`**：**sole UI atmosphere thinning read（UI 唯一终端变薄）**。叠在 **`civilizationStabilizedStructuralThinning`**（**intermediate / non-final**）之上，用 `guidanceFragmentation` 的 weather / route / single-line / minimal-ending 倾向再加一层 **默认安静**。术语见 `terminology-governance.md`。
- UI **不增加**新路由、新 onboarding、新面板；只改变既有组件如何读 thinning。

## 本文件定义什么

回答：为何网站最终会 **基础设施化（infrastructure-shaped）**— visitor（访客）感受到的是稳定默认环境，而不是「产品更新清单」。

## Phase H 落地（与代码一致）

1. **Daily**：更高 `dailyPreferUltraThin` / echo & slice closure 概率（由 combined bias 驱动）。
2. **Guidance**：更常 `guidanceStackWeatherOnly`、`hardNoRoutes`、`hardNoClosure`。
3. **Objects / Mail / Home**：更高 prose bias → maturity strip modulus bonus（模数奖励上限仍受 pass cap）。

## 代码锚点

- `src/data/world-ai-native-infrastructure-layer/extreme-invisible-infrastructure-thinning-pass.ts`
- 组件读：`worldAiNativeInfrastructure.invisibleInfrastructureStructuralThinning`

## 红线回顾

- 禁止 orchestration 可视化、禁止 meta「runtime 正在运行」、禁止 companion。
