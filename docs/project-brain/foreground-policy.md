# Foreground Policy

## What may stay loud (by design)

- Same-day **room weather** and **browser familiarity** (living presence + browser reality).
- **World breathing** and **shareable residue** (regulation breathing + low-signal traces within caps).
- **Hostname familiarity** (browser / sediment lines).

## Caps

- `world-regulation-engine` object foreground cap and `runtime-retirement-system` rare foreground objects.
- `world-density-calibration` section matrix and mutual exclusion.

## Freeze alignment

- `world-freeze-system` `stabilityMap` documents which named concerns are permanent vs semi-foreground; implementation remains in existing bundles—freeze is the **charter**, not a parallel runtime.
