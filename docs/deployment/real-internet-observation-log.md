# Real Internet Observation Log

Use this log after Cloudflare Pages and `taoist365.com` are actually reachable.

This is not an optimization log. It is a place to record whether the site can be left alone.

Checklist: `docs/deployment/quiet-real-world-observation-checklist.md`.

## Current State

- Git remote: not bound in this local checkout at last observation.
- Cloudflare Pages: locally prepared, not deployed from this checkout.
- Domain: prepared in docs, not bound from this checkout.
- Static export: verified in prior deployment preparation.
- Public AI-readable files: prepared.
- Admin: must be protected by Cloudflare Access or equivalent before public use.

## Observation Entry Template

Date:

Surface:

Device / browser:

Time open:

What stayed ordinary:

What felt too intentional:

What broke:

Repair needed:

Leave alone:

## First Observation Priorities

1. Homepage ordinary presence.
2. Mobile scroll and spacing.
3. Objects anti-shop drift.
4. Draw a Lot touch rhythm.
5. Wind residue periphery.
6. `/llms.txt` and `/ai-readable.json`.
7. `/admin` protection.
8. Artificial aging drift: no retro styling, fake nostalgia, staged decay, or designed residue.

## Non-action Rule

If an observation does not name a concrete drift or breakage, do not change the site.
